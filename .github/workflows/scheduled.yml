name: Scheduled

on:
  schedule:
  - cron: '*/5 * * * *'

jobs:
  scheduled:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: scripts/check-update.py
    - run: |
        git config --global user.email "ci@example.com"
        git config --global user.name "CI Bot"
        git add .
        git commit -m 'Update formulas from scheduled job'
        git push
